#include "Camera.hpp"

#include "Renderer.hpp"

namespace Elcarim::Objects {
	Camera::Camera() {
		//getTransformation().setPosition(Graphics::Renderer::PROJECTION_RESOLUTION_WIDTH / 2.0f, Graphics::Renderer::PROJECTION_RESOLUTION_HEIGHT / 2.0f);
	}
}
